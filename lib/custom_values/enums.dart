enum HabitStatus { doing, done, canceled, future }

enum HabitTileType { dailyProgress, general }

enum HabitModelMode { NEW, VIEW, EDIT, DUPLICATE }

enum HabitSelectedOption { CHECK, CLEAR, REFRESH, RESET, EDIT, DEL }
